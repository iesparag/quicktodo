#!/usr/bin/env node
// Entry point for quicktodo CLI.
// Parses argv and delegates to core logic in ../lib/taskManager.js (to be implemented in full later)

import { fileURLToPath } from 'url';
import { dirname } from 'path';

// Placeholder: real implementation will follow in future commits

console.log('quicktodo: CLI placeholder. Run `quicktodo help` (future) for usage.');
